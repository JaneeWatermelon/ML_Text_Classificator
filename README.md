# NLP Text Classification: Naive Bayes vs BiRNN (from scratch)

Проект посвящён **бинарной классификации** текстовых комментариев с использованием:

* **Наивного байесовского** классификатора
* Двунаправленной рекуррентной нейронной сети (**BiRNN**)

Проект выполнен в учебных и исследовательских целях и демонстрирует ручную реализацию двух алгоритмов классификации текста.

Также я описал [ноутбук в Google Colab](https://colab.research.google.com/drive/10J2a5VOyW2L-Q14WE8019bUEsh-ns-vt?usp=sharing), где можно самому "позапускать" алгоритмы, посмотреть визуализации, а также более подробно ознакомиться с теоретическим описанием, формулами и экспериментами.

## 📌 Задача

Для каждого комментария необходимо определить, должен ли он быть заблокирован:

* `0` — комментарий допустим
* `1` — комментарий должен быть заблокирован

> Задача формулируется как бинарная классификация текста.

## 🔧 Используемые технологии

* Python 3
* NumPy
* Pandas
* Gensim — предобученные GloVe-эмбеддинги
* Scikit-learn — метрики качества
* Matplotlib — визуализация ROC-кривой

## 🟦 Naive Bayes

### Описание

**Наивный байесовский классификатор** реализован для текста в предположении **условной независимости слов**:

$$
  P(x|y=k) = P(x_1,x_2,...,x_n|y=k) = \prod_{i=1}^n P(x_i|y=k)
$$

К **плюсам** данного метода можно отнести:

* Скорость алгоритма (не обучается вообще ничего)
* Интерпретируемость
* Прост в реализации и использовании

Из **минусов**:

* Модель «мешка слов» (без учёта порядка)
* Не устойчив к так называемым AML-атакам

> **Naive Bayes** обычно используется как **сильный бейзлайн**, но может быть использован как основной алгоритм, если нейросетевые подходы излишни.

## 🟧 BiRNN (Bidirectional Recurrent Neural Network)

### Архитектура

Используются **2** независимые Vanilla RNN:

* **Forward RNN** — слева направо
* **Backward RNN** — справа налево

Их финальные состояния конкатенируются:

$$ h =
  \begin{bmatrix}
  h_{fT} \\
  h_{bT}
  \end{bmatrix}
\in ℝ^{2H}
$$

Далее применяется линейный классификатор с softmax.

### Vanilla RNN (один шаг)

$$
  h_t = tanh(W_x x_t + W_h h_{t-1} + b)
$$

Обучение производится с помощью **Backpropagation Through Time** (BPTT), реализованного вручную.

## 🔤 Эмбеддинги

Для представления слов используются предобученные:

* GloVe Twitter embeddings
* размерность: `100`

> Эмбеддинги не обучаются, неизвестные слова заменяются нулевым вектором (UNK)

## 📈 Метрики качества

В проекте используются:

* Accuracy
* ROC-AUC
* ROC-кривая

> ROC-AUC считается по вероятности положительного класса (`should_ban = 1`).

## 📊 Результаты и наблюдения

### Основные выводы:

* Naive Bayes показывает сильные результаты по Accuracy
* BiRNN:
  * сложнее в обучении
  * часто показывает лучшее качество ранжирования (ROC-AUC)

> Плюсы и минусы каждого из алгоритмов были описаны выше.

## ▶️ Запуск проекта

### Установить зависимости:

`pip install -r requirements.txt`

### Запустить:

`python main.py`

### В процессе:

* будут запущены оба алгоритма:
  * Наивный Байес завершится почти сразу
  * BiRNN придётся подождать:
    * загрузка эмбеддингов
    * обучение модели
* будут выведены Accuracy и ROC-AUC
* отобразится ROC-кривая для BiRNN

## Параметры:

Можно поиграться с гиперпараметрами и сравнить результаты:

* `input_size` — размерность эмбеддингов _(фиксированная)_
* `hidden_size` — размерность скрытого состояния
* `output_size` — размерность выхода _(фиксированная = 2)_
* `n_epochs` — количество эпох
* `alpha` — скорость обучения
